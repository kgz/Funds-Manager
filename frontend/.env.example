VITE_PORT=3000
# Use HTTP when the browser can't trust mkcert (common on WSL + Windows Chrome)
VITE_DEV_HTTP=true
# VITE_API_PROXY_TARGET=https://127.0.0.1:2020
# Or provide your own certs instead of mkcert:
# VITE_HTTPS_KEY_PATH=./localhost-key.pem
# VITE_HTTPS_CERT_PATH=./localhost.pem
