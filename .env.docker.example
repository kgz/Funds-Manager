# Docker Compose — copy to .env to override
# Default matches local dev Postgres (database/docker-compose.yml on port 5434)
DATABASE_URL=postgres://funds:funds@host.docker.internal:5434/funds?sslmode=disable
