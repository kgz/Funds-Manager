## What Changes

Warn before replacing an existing statement for the same account + month. Two-phase upload: preview conflicts, confirm replace.

## Capabilities

- `statements` — preview query param, replace requires explicit confirm
- `frontend` — replace confirmation modal on `/statements`
