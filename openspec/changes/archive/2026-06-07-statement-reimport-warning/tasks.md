## 1. Backend

- [x] 1.1 `POST /api/statements?preview=true` returns conflicts without persisting
- [x] 1.2 `POST /api/statements?replace=true` replaces existing period
- [x] 1.3 Default import rejects duplicate period without replace flag

## 2. Frontend

- [x] 2.1 Preview upload before import
- [x] 2.2 Modal warns with period + account; Cancel / Replace actions
