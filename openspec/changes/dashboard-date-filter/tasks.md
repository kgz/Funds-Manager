## 1. Filter logic

- [ ] 1.1 Define `DateRange` type and filter helper on `transaction_date`
- [ ] 1.2 Add state + localStorage persistence

## 2. UI

- [ ] 2.1 Add pill selector in dashboard header
- [ ] 2.2 Pipe filtered transactions into all dashboard memos (pies, bar, line, KPIs)

## 3. Edge cases

- [ ] 3.1 Empty filtered set shows empty state (reuse ux-polish component)
- [ ] 3.2 Label KPI cards with active period context (e.g. "This month")
