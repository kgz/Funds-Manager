## ADDED Requirements

### Requirement: Balance chart clarity
The balance-over-time chart SHALL display at most one reference line (average balance). Min and max reference lines MUST NOT be shown on the chart.

#### Scenario: Simplified reference lines
- **WHEN** user views the balance chart with data
- **THEN** only the balance line and optional average reference are visible
