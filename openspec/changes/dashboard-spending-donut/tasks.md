## 1. Donut

- [ ] 1.1 Add `variant` prop to `CategoryPieChart` with innerRadius for donut
- [ ] 1.2 Render centre label with total spend (sum of slice values)
- [ ] 1.3 Apply donut variant to spending chart only

## 2. Legend

- [ ] 2.1 Replace vertical legend with compact ranked list under chart (name, amount, %)
- [ ] 2.2 Truncate to top 5 categories; show remainder as "Other" if needed
